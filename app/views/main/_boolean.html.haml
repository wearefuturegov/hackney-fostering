.input_container{ class: "#{question['classes']} " + (question['with_title'] ? "with_title" : "") }
  = form.input question['name'], as: :radio_buttons, collection: [['Yes', true], ['No', false]], label: question['title']
