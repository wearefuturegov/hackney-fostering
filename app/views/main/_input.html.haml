-if question['current_section']
  .gds_sub_title=question['current_section']
.input_container{ class: "#{question['classes']} " + (question['with_title'] ? "with_title" : "") }
  = form.input question['name'], as: :string, label: question['title'], input_html: { class: question['name'] }

