%section.section
  %h1
    = question['heading']
  %p
    = question['intro']

  %p
    = question['extra']

  / %p.box
  /   = question['notice']

%h2
  = question['title'] % { name: child.first_name, number: application.children_elsewhere_count }
